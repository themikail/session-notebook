git pull // dateien runterziehen

git pull --no-ff // wenn datein geändert wurden in github und über terminal gepusht wurde. Merge fehler

git add . //hinzufügen

git commit -m "COMMIT" //kommentieren

git push //hochladen